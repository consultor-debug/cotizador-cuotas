# Publica esta carpeta tal cual en Netlify (sitio estático, sin build).
[build]
  publish = "."
  command = ""

# Cache largo para los recursos versionados (?v=NN), sin cachear el HTML.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
